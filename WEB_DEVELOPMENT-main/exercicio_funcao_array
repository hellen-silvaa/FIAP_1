// 1-dobrar os numeros do array
// function dobrar(num) {
//     return num.map (valor => valor * 2);
// }

// let resultado = dobrar ([2,4,10,20]);
// console.log(resultado);


//2 - gritando alto
function grande(letra) {
    return letra.map 
    ( 
        letraHellen => {
        return letraHellen.toUpperCase(); 
    });
}

console.log(grande(["oi", "tudo", "bem"]));


